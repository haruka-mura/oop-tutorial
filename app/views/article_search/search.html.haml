%h1 検索フォーム

= form_with(model: @article_search_form, url: search_path, local: true, method: (:get)) do |f|
  -# - if @article_search_query.errors.any?
  -#   #error_explanation
  -#     %h2= "#{pluralize(@article_search_query.errors.count, "error")} prohibited this article from being saved:"
  -#     %ul
  -#       - @article_search_query.errors.full_messages.each do |message|
  -#         %li= message

  .field
    = f.label :keyword, "キーワード"
    = f.text_field :keyword
  .actions
    = f.submit '検索'


%h1 検索結果

%table
  %thead
    %tr
      %th Title
      %th ID
      %th
      %th
      %th

  %tbody
    - @article_search_form.articles.each do |article|
      %tr
        %td= article.title
        %td= article.id
        %td= link_to 'Show', article
        %td= link_to 'Edit', edit_article_path(article)
        %td= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' }

%br
