= form_for @articles_category do |f|
  - if @articles_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@articles_category.errors.count, "error")} prohibited this articles_category from being saved:"
      %ul
        - @articles_category.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :article
    = f.text_field :article
  .field
    = f.label :category
    = f.text_field :category
  .actions
    = f.submit 'Save'
